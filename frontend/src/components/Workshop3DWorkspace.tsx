/**
 * Compatibility export kept for integrations that imported the former
 * 3D-only shell. The workshop now uses one shared 2D/3D workspace.
 */
export { WorkshopWorkspace as Workshop3DWorkspace } from './WorkshopWorkspace';
