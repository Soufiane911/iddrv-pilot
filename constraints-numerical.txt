# Shared training/inference environment of the approved summary6 package.
# Python must separately match its manifest exactly (currently 3.13.9).
# Do not upgrade independently: revalidate artifacts and strict loaders first.
numpy==2.2.6
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.7.2
joblib==1.5.2
